[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]

[phases.install]
cmds = ["pnpm install --filter @wacent/api..."]

[phases.build]
cmds = ["pnpm --filter @wacent/api build"]

[start]
cmd = "pnpm --filter @wacent/api start"
