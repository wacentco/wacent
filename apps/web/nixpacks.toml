[phases.setup]
nixPkgs = ["nodejs_20"]
cmds = ["npm install -g pnpm@9.15.9"]

[phases.install]
cmds = ["pnpm install --filter @wacent/web..."]

[phases.build]
cmds = ["pnpm --filter @wacent/web build"]

[start]
cmd = "pnpm --filter @wacent/web start"
