services:
  worker:
    build:
      context: .
      dockerfile: apps/worker/Dockerfile.vultr
    restart: always
    ports:
      - "3001:3001"
    env_file: .env.production

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/worker.conf:/etc/nginx/conf.d/default.conf
      - ./certbot/conf:/etc/letsencrypt
    depends_on:
      - worker
